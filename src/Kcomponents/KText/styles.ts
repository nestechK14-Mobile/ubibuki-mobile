import { StyleSheet } from 'react-native';
import { COLORS } from 'Kconstants';
import { FONTS } from 'Kconstants';
const styles = StyleSheet.create({
  textDefault: {
    color: COLORS.BLACK,
    fontFamily: FONTS.REGULAR
  }
});

export default styles;