import React, { memo } from 'react';
import { View, Text } from 'react-native';

const MHeader = () => {
  return (
    <View>
      <Text>Header</Text>
    </View>
  );
};

export default memo(MHeader);
