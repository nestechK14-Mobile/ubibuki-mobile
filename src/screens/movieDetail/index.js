import { MText, MView } from '@/components';
import React from 'react';
import styles from './styles';

const MovieDetailScreen = () => {
  return (
    <MView style={styles.container}>
      <MText>Movie Detail Screen</MText>
    </MView>
  );
};

export default MovieDetailScreen;
