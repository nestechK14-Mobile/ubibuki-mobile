import { MText, MView } from '@/components/common';
import React from 'react';
import styles from './styles';

const MoviesScreen = () => {
  return (
    <MView style={styles.container}>
      <MText>MoviesScreen</MText>
    </MView>
  );
};

export default MoviesScreen;
