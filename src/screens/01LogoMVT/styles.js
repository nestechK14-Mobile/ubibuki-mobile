import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonSkip: {
    fontSize: 36,
    fontWeight: '600',
    marginLeft: 300,
    marginTop: 30
  }
});

export default styles;
